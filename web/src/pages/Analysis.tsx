import type { Snapshot } from '../types'; import { Icon } from '../icons';
const money=(n:number)=>`¥ ${n.toLocaleString('zh-CN',{minimumFractionDigits:2,maximumFractionDigits:2})}`;
export function Analysis({state}:{state:Snapshot}){const a=state.analysis;return <div className="page"><div className="page-title"><div><h1>财务分析</h1><p>数字负责陈述事实，LedgerX 尽量不负责制造焦虑。</p></div></div><div className="analysis-grid"><section className="panel verdict"><span className="large-icon"><Icon name="shield" size={25}/></span><div><small>当前判断</small><h2>{a.verdict}</h2></div></section><section className="panel stat"><small>累计流入</small><b>{state.hideAllAmounts?'••••••':money(a.income)}</b></section><section className="panel stat"><small>固定成本占比</small><b>{state.hideAllAmounts?'••••••':`${a.fixedCostRatio.toFixed(1)}%`}</b></section><section className="panel stat"><small>财务依赖度</small><b>{state.hideAllAmounts?'••••••':`${a.dependency.toFixed(1)}%`}</b></section></div><section className="panel prose"><h2>怎么看这些数</h2><p>固定成本 {state.hideAllAmounts?'••••••':money(a.fixedCost)}，可变成本 {state.hideAllAmounts?'••••••':money(a.variableCost)}。财务依赖度按分类中包含“父母”的收入估算；它是观察项，不是道德评分。</p><p>先保证现金安全垫，再处理长期固定成本。比起月底对账时突然顿悟，提前知道资金链还有多长更有用。</p></section></div>}

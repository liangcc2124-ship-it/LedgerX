import type { Snapshot } from '../types'; import { Icon } from '../icons';
export function Data({state,onCommand}:{state:Snapshot;onCommand:(c:string)=>Promise<void>}){return <div className="page"><div className="page-title"><div><h1>数据管理</h1><p>没有账号，没有云端，也没有突然出现的会员连续包月。</p></div></div><section className="panel data-card"><span className="large-icon"><Icon name="data" size={26}/></span><div><h2>本地账本</h2><p>所有记录只保存在这台电脑。建议偶尔导出一份备份——硬盘通常不会提前请假。</p><code>{state.dataFile}</code><div className="button-row"><button className="button primary" onClick={()=>onCommand('backup')}>导出备份</button><button className="button ghost" onClick={()=>confirm('恢复会替换当前数据，继续吗？')&&onCommand('restore')}>从备份恢复</button></div></div></section></div>}

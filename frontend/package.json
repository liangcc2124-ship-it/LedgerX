{
  "name": "ledgerx-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": "22.18.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:config": "node --test scripts/vite-config.test.mjs",
    "test": "npm run test:config && npm run build && node scripts/run-playwright.mjs"
  },
  "dependencies": {
    "vue": "3.5.42"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "@vitejs/plugin-vue": "6.0.8",
    "vite": "8.2.2"
  }
}
